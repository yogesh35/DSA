#User function Template for python3

class Solution:
    def rotate(self, arr):
        
        i, j = 0, len(arr) - 1
        while i != j:
            arr[i], arr[j] = arr[j], arr[i]
            
            i += 1
        
